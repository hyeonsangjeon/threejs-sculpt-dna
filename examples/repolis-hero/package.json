{
  "name": "repolis-tree-flagship",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 4174 --bind 127.0.0.1 --directory .",
    "dev": "vite --host 127.0.0.1 --port 4174",
    "build": "vite build --base /threejs-sculpt-dna/",
    "preview": "vite preview --host 127.0.0.1 --port 4174",
    "capture": "node scripts/capture.mjs",
    "test:capture": "node --test scripts/capture-isolation.test.mjs"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "puppeteer-core": "25.3.0",
    "vite": "^7.3.6"
  }
}
